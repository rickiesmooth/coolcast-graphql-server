{
    "name": "@coolcast/apollo-prisma",
    "version": "0.0.2",
    "private": false,
    "scripts": {
        "start": "node dist/index.js",
        "build": "ncc build index.ts --no-minify",
        "generate-prisma-client": "cd src/prisma && prisma generate",
        "prisma-deploy": "cd prisma && prisma deploy",
        "generate": "graphqlgen"
    },
    "dependencies": {
        "@zeit/ncc": "^0.5.5",
        "prisma": "^1.22.2"
    },
    "devDependencies": {
        "graphqlgen": "^0.4.0"
    }
}
